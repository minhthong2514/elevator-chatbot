{
  "name": "web-root",
  "version": "1.0.0",
  "scripts": {
    "dev": "concurrently \"cd src/frontend && npm start\" \"cd src/backend && node server.js\" \"cd src/backend && ../../venv/bin/python3 main_vision.py\" \"cd src/backend && ../../venv/bin/python3 main_chatbot.py\"",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  },
  "dependencies": {
    "cleave.js": "^1.6.0",
    "dotenv": "^17.3.1",
    "lucide-react": "^0.577.0",
    "react-router-dom": "^7.13.1",
    "sass": "^1.98.0"
  }
}
